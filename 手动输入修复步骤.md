# 手动输入修复步骤（解决换行符丢失问题）

如果复制粘贴 SQL 时换行符丢失，请**手动在 Supabase SQL Editor 中输入**以下命令：

## 步骤 1: 创建函数

**重要：请逐行手动输入，每行后按回车键换行**

在 Supabase SQL Editor 中，**手动输入**以下内容（注意：每个关键字之间要有空格或换行）：

```
CREATE OR REPLACE FUNCTION is_admin()
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO public
AS $$
BEGIN
  RETURN EXISTS (
    SELECT 1 FROM users
    WHERE id = auth.uid() AND role = 'admin'
  );
END;
$$;
```

**输入技巧：**
1. 每次输入一个关键字后按空格
2. 每行结束后按回车换行
3. 确保 `LANGUAGE` 和 `plpgsql` 之间有空格
4. 确保 `SECURITY` 和 `DEFINER` 之间有空格
5. 确保 `SET` 和 `search_path` 之间有空格

输入完成后，点击 **Run** 按钮执行。

---

## 步骤 2: 删除旧策略

手动输入：

```
DROP POLICY IF EXISTS "Admins can read all users" ON users;
```

点击 **Run** 执行。

---

## 步骤 3: 创建新策略

手动输入：

```
CREATE POLICY "Admins can read all users" ON users
  FOR SELECT USING (is_admin());
```

点击 **Run** 执行。

---

## 步骤 4: 验证

执行以下查询验证：

```
SELECT is_admin();
```

如果返回 `false` 或 `true`，说明函数创建成功！

---

## 备选方案：使用格式化后的单行版本

如果手动输入仍然困难，尝试这个**单行版本**（所有关键字之间都有空格）：

```
CREATE OR REPLACE FUNCTION is_admin() RETURNS BOOLEAN LANGUAGE plpgsql SECURITY DEFINER SET search_path TO public AS $$ BEGIN RETURN EXISTS ( SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin' ); END; $$;
```

直接复制这一整行到 SQL Editor，应该可以正常执行。

