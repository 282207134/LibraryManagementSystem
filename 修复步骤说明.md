# 修复 RLS 策略 - 分步执行指南

如果直接复制 SQL 文件时出现语法错误（换行符丢失），请：

1. **优先使用**：`手动输入修复步骤.md` 文件中的指南，手动输入 SQL
2. **或使用**：下面的分步执行方案

## 步骤 1: 创建 is_admin() 函数

**一次性复制以下完整代码块**（包括所有换行）：

```sql
CREATE OR REPLACE FUNCTION is_admin()
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO public
AS $$
BEGIN
  RETURN EXISTS (
    SELECT 1 FROM users
    WHERE id = auth.uid() AND role = 'admin'
  );
END;
$$;
```

**执行后应显示：** `Success. No rows returned`

---

## 步骤 2: 删除旧的策略

```sql
DROP POLICY IF EXISTS "Admins can read all users" ON users;
DROP POLICY IF EXISTS "Admins can view all borrowing records" ON borrowing_records;
```

**执行后应显示：** `Success. No rows returned`

---

## 步骤 3: 创建新的策略

**第一次执行：**

```sql
CREATE POLICY "Admins can read all users" ON users
  FOR SELECT USING (is_admin());
```

**第二次执行：**

```sql
CREATE POLICY "Admins can view all borrowing records" ON borrowing_records
  FOR SELECT USING (is_admin());
```

---

## 验证

执行以下查询验证函数是否创建成功：

```sql
SELECT is_admin();
```

如果返回 `false`（你不是管理员）或 `true`（你是管理员），说明函数创建成功！

---

## 如果仍然失败

如果上面的步骤仍然报错，请尝试**最简单的单行版本**：

```sql
CREATE OR REPLACE FUNCTION is_admin() RETURNS BOOLEAN LANGUAGE plpgsql SECURITY DEFINER SET search_path TO public AS $$ BEGIN RETURN EXISTS (SELECT 1 FROM users WHERE id = auth.uid() AND role = 'admin'); END; $$;
```

然后再执行策略相关的 SQL。

