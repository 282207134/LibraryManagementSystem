# 图书管理系统项目设计书（当前实现）

## 1. 项目定位

面向读者与管理员的图书管理系统，提供图书浏览、借阅归还、收藏评论，以及后台图书/借阅管理。

## 2. 技术架构

- 前端：React 19 + TypeScript + Vite 7 + Tailwind CSS 4
- 路由：React Router 7
- 后端：Supabase（Auth、PostgreSQL、Storage、Edge Functions）
- 状态：React Hooks + Context（`AuthContext`、`LanguageContext`）

## 3. 角色与权限

- `user`：用户端功能（浏览、借阅、收藏、评论、个人资料）
- `admin`：拥有 `user` 全部权限 + 管理端访问权限

权限控制双层：

1. 前端路由守卫（体验层）
2. RLS / RPC（安全层，最终生效）

## 4. 路由设计（落地版本）

### 未登录

- `/reset-password`：重置密码页面
- 其他路径：显示登录/注册页（组件内切换模式）

### 已登录

- `/user/dashboard`
- `/user/home`、`/user/books`
- `/user/books/:id`
- `/user/my-borrowings`
- `/user/my-favorites`
- `/user/profile`

### 管理端（仅 admin）

- `/admin/dashboard`
- `/admin/borrowings`

## 5. 核心模块

- `App.tsx`：登录态分流与总路由
- `AdminApp.tsx`：管理端子路由
- `useBooks`：图书分页、检索、CRUD、封面协同
- `useBorrowings`：借阅/归还 RPC、记录读取
- `useFavorites`：收藏增删查
- `useReviews`：评论评分
- `useHomeData`：首页推荐/排行/分类聚合
- `ChatAssistant`：AI 助手入口与交互

## 6. 数据模型

主要实体：

- `books`
- `users`
- `borrowing_records`
- `book_favorites`
- `reviews`

关键函数：

- `is_admin()`
- `borrow_book(p_book_id, p_user_id, p_days)`
- `return_book(p_borrowing_id)`

## 7. 关键业务流程

### 认证

`AuthContext` 初始化会话并加载用户资料，失败时做降级处理，避免页面长期卡在加载态。

### 图书分页检索

`useBooks` 先查 `count` 再 `range`，支持 `title/author` 模糊查询，按 `created_at` 倒序。

### 借阅与归还

前端仅调用 RPC，数据库函数保证库存与记录状态更新的原子性。

### 封面管理

上传成功后写表；写表失败回滚上传文件；替换时清理旧文件。

## 8. AI 助手设计

- Edge Function：`ai-chat`
- 图书意图问题返回书目卡片 + `bookBatch` 分页信息
- 非图书闲聊返回普通对话文本
- 支持 DeepSeek / Ollama 作为模型提供方

## 9. 非功能目标

- 安全：RLS 与最小权限原则，前端不保存高权限密钥
- 性能：分页、索引、减少冗余请求
- 可维护性：hooks 分层 + 明确类型定义 + 统一文档

## 10. 里程碑建议

1. 核心链路自动化测试（登录、借阅、归还、收藏）
2. 管理端审计与库存预警
3. 通知系统（到期提醒）与推荐质量优化
