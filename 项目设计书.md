# 图书管理系统项目设计书

## 1. 文档信息

| 项目 | 内容 |
|------|------|
| 项目名称 | 图书管理系统（Library Management System） |
| npm 包名 | `books-management-system` |
| 文档版本 | v1.1 |
| 编写日期 | 2026-04-03 |
| 修订说明 | v1.1：与当前仓库实现一致（路由、RPC 参数、React 19、模块清单、章节层级） |

**技术栈**：React 19 + TypeScript + Vite 7 + Tailwind CSS 4 + React Router 7 + Supabase（PostgreSQL + Auth + Storage）

**相关文档**（仓库内）：

- [README.md](./README.md) — 快速开始与脚本说明  
- [部署指南.md](./部署指南.md) — 部署与 Supabase 初始化  
- [DATABASE.md](./DATABASE.md) — 表结构与 RLS 示例  
- [USER_INTERFACE_DESIGN.md](./USER_INTERFACE_DESIGN.md) — 用户端界面与权限细化说明（部分路由描述与现网入口不同，以本文「路由」为准）  
- [SUPABASE_TABLES_PROPOSAL.md](./SUPABASE_TABLES_PROPOSAL.md) — 表与策略提案  

---

## 2. 项目背景与目标

本项目构建面向**读者**与**管理员**的图书管理平台，覆盖图书浏览、借阅与归还、收藏、评论评分、用户认证与后台管理。

**核心目标**：

1. 图书业务流程清晰、可扩展。  
2. 借助 Supabase 提供认证、关系型数据与对象存储。  
3. 前后端职责边界明确：业务规则尽量下沉数据库（RPC/RLS）。  
4. 通过 RLS 与角色控制保障数据安全。  

---

## 3. 范围定义

### 3.1 本期功能范围（In Scope）

- 用户注册、登录、登出、找回密码（`/reset-password`）。  
- **用户端**（`/user/*`）：仪表盘、首页/书库、图书详情、我的借阅、我的收藏、个人资料。  
- **管理端**（`/admin/*`，仅 `users.role = admin`）：图书 CRUD、检索分页、借阅记录管理。  
- 图书封面上传、更新、删除（Storage 桶 `book-covers`，见 `lib/storageHelper.ts`）。  
- 借阅与归还通过数据库 RPC（`borrow_book` / `return_book`）保证原子性。  
- 收藏、图书详情页评论与评分（`reviews` / `useReviews`）。  
- 首页聚合数据（推荐、人气、新书、分类等，`useHomeData`）。  

### 3.2 非目标（Out of Scope）

- 多租户 / 多图书馆隔离。  
- 复杂推荐算法（机器学习）。  
- 支付、罚金等财务流程。  
- 外部编目系统自动同步。  

---

## 4. 用户角色与权限模型

### 4.1 角色定义

- `user`：浏览、借阅、收藏、评论、维护个人资料。  
- `admin`：具备上述能力，且可进入 `/admin/*` 管理图书与借阅记录。  

角色存于公共表 `users.role`，与 `auth.users` 一对一；前端用 `userRole === 'admin'` 做路由守卫，**最终以 RLS 与 RPC 为准**。  

### 4.2 权限原则

- 未登录：仅登录/注册与密码重置；不进入 `/user` 或 `/admin` 业务壳层。  
- 已登录：默认进入用户区；管理员额外可访问管理区。  
- 敏感写操作走 RPC，避免多请求竞态。  

---

## 5. 总体架构设计

### 5.1 逻辑架构

1. **表现层**：React 页面与组件。  
2. **应用层**：Hooks + Context（认证与业务数据）。  
3. **数据访问层**：`supabase` 客户端（表查询、RPC、Storage）。  
4. **数据层**：PostgreSQL + Storage；Auth 由 Supabase 托管。  

### 5.2 关键模块与源码映射

| 职责 | 路径 |
|------|------|
| 应用入口、Auth 包裹 | `src/main.tsx` |
| 路由总线、登录态分流、管理端守卫 | `src/App.tsx` |
| 全局认证与资料、角色 | `src/contexts/AuthContext.tsx`、`AuthContextBase.ts` |
| 图书分页/检索/CRUD、封面上传协调 | `src/hooks/useBooks.ts` |
| 借阅/归还 RPC、记录查询 | `src/hooks/useBorrowings.ts` |
| 收藏 | `src/hooks/useFavorites.ts` |
| 首页聚合 | `src/hooks/useHomeData.ts` |
| 评论与评分 | `src/hooks/useReviews.ts` |
| Supabase 客户端 | `src/lib/supabaseClient.ts` |
| 封面 Storage | `src/lib/storageHelper.ts` |
| 管理端子应用路由 | `src/AdminApp.tsx` |
| 用户布局与导航 | `src/components/user/UserLayout.tsx`、`UserHeader.tsx` |

### 5.3 路由与页面结构（与实现一致）

- **未登录**：除 `/reset-password` 外，通配路由渲染 `Login` / `Register`（由本地 `authMode` 切换），**无** `/user/login` 独立路径。  
- **已登录**  
  - `/user` → `UserLayout`  
    - `/user` → 重定向到 `/user/dashboard`  
    - `/user/dashboard` — 仪表盘  
    - `/user/home`、`/user/books` — 书库（同一页 `UserHome`）  
    - `/user/books/:id` — 详情 `UserBookDetail`  
    - `/user/my-borrowings`、`/user/my-favorites`、`/user/profile`  
  - `/admin/*` — 非 admin 重定向至 `/user/dashboard`  
    - `/admin/dashboard` — 图书管理  
    - `/admin/borrowings` — 借阅管理  
  - `/`、`/*` — 重定向到 `/user/dashboard`  

---

## 6. 数据库设计

### 6.1 主要实体

1. `books` — 图书主数据（含 `quantity` / `available_quantity`、封面 URL 等）。  
2. `users` — 与 `auth.users` 绑定的扩展资料（含 `role`、`max_borrow_limit` 等）。  
3. `borrowing_records` — 借阅流水（`borrowed` / `returned` / `overdue` / `lost`）。  
4. `book_favorites` — 用户-图书收藏。  
5. `reviews` — 评分与评论（用于详情与统计）。  

### 6.2 核心约束建议

- `books.available_quantity <= books.quantity`。  
- `books.isbn` 唯一（允许空）。  
- 借阅状态枚举与业务规则一致；归还后更新库存。  
- `users.role` 限定为 `user` / `admin`（与前端类型一致）。  

### 6.3 索引建议

- `books`：`title`、`author`、`category`（视查询而定）。  
- `borrowing_records`：`user_id`、`book_id`、`status`。  
- `book_favorites`：`(user_id, book_id)` 唯一。  

### 6.4 借阅相关 RPC（与前端一致）

前端 `useBorrowings` 调用：

- **`borrow_book`**  
  - 参数：`p_book_id`、`p_user_id`、`p_days`（默认借阅天数由前端常量 `DEFAULT_BORROW_DAYS = 30` 传入）。  
  - 期望返回：含 `success: true` 与可选 `due_date`；失败时 `success: false` 或 `error` 字段。  

- **`return_book`**  
  - 参数：`p_borrowing_id`。  
  - 期望返回：与借阅类似的 success / error 约定。  

具体函数体需在 Supabase SQL 中实现并保持与上述契约一致。  

---

## 7. 核心业务流程设计

### 7.1 认证初始化流程

1. 启动时 `getSession`，校验过期则 `signOut`。  
2. 有效会话则加载 `users` 行；缺失则插入默认资料（见 `AuthContext`）。  
3. `onAuthStateChange` 统一同步 session；对 `TOKEN_REFRESHED`、重复 `SIGNED_IN` 做降噪。  
4. 资料加载设短超时与全局最长 loading 超时，避免白屏卡死。  

### 7.2 图书查询与分页流程

1. 先 `count` 再 `range` 分页，每页条数由 `useBooks` 内 `PAGE_SIZE` 定义。  
2. 搜索：`title` / `author` 的 `ilike` 组合条件。  
3. 排序：`created_at`、`id` 降序，保证顺序稳定。  

### 7.3 借阅与归还流程

1. 前端仅调用 RPC，展示成功/失败信息。  
2. 后端在事务内更新 `borrowing_records` 与 `books.available_quantity`。  

### 7.4 封面上传与删除流程

1. 校验格式与大小（表单/组件层）。  
2. 上传至 `book-covers`；优先签名 URL，必要时回退 public URL。  
3. 更新图书失败时回滚新文件；替换封面时删除旧存储对象（见 `storageHelper` / `useBooks` 协调逻辑）。  

---

## 8. 前端工程设计

### 8.1 目录分层

- `pages/` — 页面级容器。  
- `components/` — 通用与管理端 UI；`components/user/` — 用户端。  
- `hooks/` — 数据与业务逻辑。  
- `contexts/` — 全局认证。  
- `types/` — TypeScript 类型。  
- `lib/` — SDK 与工具。  

### 8.2 状态管理策略

- 以 Hooks + Context 为主，无额外全局状态库。  
- 认证集中在 `AuthContext`；各业务 hook 自持 `loading` / `error` / 数据。  

---

## 9. 非功能设计

### 9.1 安全

- 生产环境启用 RLS；仅使用 anon key。  
- 管理权限以后端策略为准，前端路由仅为体验层。  

### 9.2 性能

- 列表分页；首页模块可并行请求。  
- 数据库索引与查询模式对齐。  

### 9.3 可维护性

- 模块化 hooks；TypeScript 约束；关键路径保留中文注释。  

---

## 10. 部署与运维设计

### 10.1 环境依赖

- Node.js 16+（建议与团队 CI 对齐）。  
- Supabase：Database + Auth + Storage。  
- 环境变量：`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`。  

### 10.2 标准部署流程

1. 建表、RLS、RPC、`book-covers` 桶与策略。  
2. `npm run build` 产出静态资源。  
3. 托管至 Vercel / Netlify 等。  

### 10.3 监控与排障

- Supabase Logs（Auth / API / DB）。  
- 关注 RLS、超时、Storage 策略导致的 401/403。  

---

## 11. 风险与应对

| 风险 | 应对 |
|------|------|
| RLS 配置错误 | 按表逐项验收策略；用测试账号验证 |
| 借阅并发 | 统一 RPC 事务 |
| 封面不可访问 | 部署检查 Storage 策略与桶名 |
| 认证事件抖动 | 监听内过滤重复 SIGNED_IN、合理 loading 超时 |

---

## 12. 后续演进路线

### Phase 1（短期）

- 统一错误码与用户提示映射。  
- 核心路径 E2E（登录、借阅、归还、收藏）。  

### Phase 2（中期）

- 管理端库存预警与操作审计。  
- 首页统计下沉为数据库视图或函数。  

### Phase 3（长期）

- 到期/逾期通知。  
- 多馆藏或组织隔离模型。  

---

## 13. 验收标准（DoD）

- 新环境可按 [部署指南.md](./部署指南.md) 完成配置并成功登录。  
- 普通用户与管理员路由、重定向符合第 5.3 节描述。  
- 图书 CRUD、借阅归还、收藏、评论在主流程可用。  
- 封面上传、替换、删除闭环正常。  
- 生产构建无阻断性错误。  

---

## 附录 A：扩展为评审版时可增内容

- 用例与参与者矩阵、接口契约表（含 RPC 返回 JSON Schema）。  
- 认证与借阅时序图（Mermaid）、ER 图。  
- 性能与容量假设、灾备与备份 RPO/RTO。  

（本文已覆盖与当前代码一致的设计基线；细化评审材料可在上述附录继续展开。）
