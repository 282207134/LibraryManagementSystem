# 用户界面设计文档（当前实现）

## 1. 设计目标

用户界面面向普通读者与管理员（管理员可同时使用用户端），强调：

- 图书发现效率（搜索、详情、推荐、AI 助手）
- 借阅操作闭环（借阅、到期提示、归还）
- 收藏与评论互动
- 多语言体验（中/英/日）

## 2. 信息架构

### 一级导航（用户端）

- 仪表盘
- 图书浏览
- 我的借阅
- 我的收藏
- 个人资料

### 管理端入口（仅管理员）

- 管理后台（图书管理）
- 借阅管理

## 3. 页面与交互

### `UserDashboard`

- 展示推荐图书、人气图书、新书、分类入口
- 入口组件：`BookCarousel`、`BookRanking`

### `UserHome`

- 图书浏览入口页
- 列表支持分页与关键词查询

### `UserBookDetail`

- 展示图书详情、库存、评分、评论
- 支持借阅、收藏、评论提交

### `UserMyBorrowings`

- 标签切换：当前借阅 / 历史 / 全部
- 借阅状态显示与归还操作
- 多语言提示与日期本地化

### `UserFavorites`

- 收藏列表、取消收藏、跳转详情

### `UserProfile`

- 账户信息与个人资料编辑
- 密码修改流程
- 借阅/收藏统计信息

## 4. UI 设计原则

- 深色主题为默认视觉，同时支持浅色主题偏好
- 卡片 + 分页结构，避免长列表滚动疲劳
- 统一状态反馈：加载中、空态、错误态、成功提示
- 表单控件统一圆角和焦点状态，提升可读性

## 5. 多语言设计

由 `LanguageContext` 统一管理语言状态：

- `zh`（默认）
- `en`
- `ja`

页面和组件内部使用 `textMap` 进行文案映射，并按语言切换日期格式与语音 locale。

## 6. 可访问性与易用性

- 操作按钮设置明确 `aria-label`（分页、语音、朗读等）
- 关键操作保留确认提示（例如归还）
- 文本与交互区保持对比度，兼顾深浅主题

## 7. 与后端契约

- 借阅/归还必须通过 RPC
- 收藏与评论按当前登录用户 ID 操作
- 用户资料与角色从 `users` 表读取

## 8. 现状与后续优化

已完成：

- 用户端全链路页面
- 管理端借阅页与分页组件复用
- 多语言覆盖（持续补充中）
- AI 助手与书目推荐卡片

可优化方向：

- 用户端列表筛选维度扩展（分类/状态）
- 评论排序与筛选
- 键盘无障碍增强与更多可访问性标注
