# 调试管理员界面问题

如果访问 `http://localhost:5173/admin` 时一直显示"加载中..."，请按照以下步骤排查：

## 步骤 1: 打开浏览器控制台检查错误

1. 按 `F12` 打开开发者工具
2. 切换到 **Console（控制台）** 标签
3. 刷新页面 `http://localhost:5173/admin`
4. 查看是否有红色错误信息

**重点查找：**
- `加载用户信息失败` - 说明查询 users 表出错
- `Failed to load resource: the server responded with a status of 500` - 服务器错误
- `Failed to load resource: the server responded with a status of 403` - 权限错误

## 步骤 2: 验证 is_admin() 函数是否存在

在 Supabase Dashboard 的 SQL Editor 中执行：

```sql
SELECT is_admin();
```

**如果报错**：说明函数不存在，需要执行 `修复RLS策略.sql`

**如果返回 `false` 或 `true`**：说明函数存在且正常工作

## 步骤 3: 验证你的用户角色

在 Supabase Dashboard 的 **Table Editor** 中：

1. 选择 `users` 表
2. 找到你的用户记录（根据 email 查找）
3. 确认 `role` 列的值是 `admin`（不是 `user`）
4. 如果不是，手动修改为 `admin`

## 步骤 4: 验证 RLS 策略

在 SQL Editor 中执行以下查询测试：

```sql
-- 查看当前用户是否可以读取 users 表
SELECT * FROM users WHERE id = auth.uid();
```

**如果返回 500 错误**：说明 RLS 策略有问题，需要重新执行修复脚本

**如果返回空结果**：说明你的用户不在 users 表中，需要先注册/登录

**如果返回数据**：说明策略正常

## 步骤 5: 重新执行修复脚本

如果步骤 2 中 `is_admin()` 函数不存在，请：

1. 在 Supabase Dashboard 打开 **SQL Editor**
2. 按照 `修复步骤说明.md` 中的步骤分步执行
3. 确保每一步都显示 `Success`

## 步骤 6: 清除缓存并重新登录

1. 在浏览器中按 `Ctrl + Shift + Delete` 清除缓存
2. 或者使用无痕模式访问
3. 重新登录你的账户
4. 再次访问 `http://localhost:5173/admin`

## 步骤 7: 检查网络请求

1. 在开发者工具中切换到 **Network（网络）** 标签
2. 刷新页面
3. 查找请求到 `/rest/v1/users` 的请求
4. 查看该请求的：
   - **Status Code**：应该是 200，如果是 500 说明服务器错误
   - **Response**：查看返回的内容

## 常见错误和解决方案

### 错误：`加载用户信息失败: Object`

**原因**：查询 users 表失败，可能是 RLS 策略问题

**解决**：
1. 重新执行 `修复RLS策略.sql`
2. 确保 `is_admin()` 函数已创建
3. 清除缓存并重新登录

### 错误：`Failed to load resource: 500`

**原因**：RLS 策略递归问题

**解决**：执行修复脚本，确保使用 `is_admin()` 函数而不是直接查询 users 表

### 页面一直加载，控制台没有错误

**原因**：可能是 `loadUserProfile` 函数抛出异常但没有被捕获

**解决**：
1. 检查浏览器控制台是否有隐藏的错误
2. 尝试在控制台手动执行：
   ```javascript
   // 在浏览器控制台执行
   localStorage.clear();
   location.reload();
   ```

## 如果所有步骤都失败

如果以上步骤都无法解决问题，请在 Supabase Dashboard 的 SQL Editor 中执行以下**完整重置脚本**：

```sql
-- 1. 删除所有策略
DROP POLICY IF EXISTS "Users can read own data" ON users;
DROP POLICY IF EXISTS "Users can update own data" ON users;
DROP POLICY IF EXISTS "Users can insert own data" ON users;
DROP POLICY IF EXISTS "Admins can read all users" ON users;

-- 2. 重新创建 is_admin 函数
CREATE OR REPLACE FUNCTION is_admin()
RETURNS BOOLEAN
LANGUAGE plpgsql
SECURITY DEFINER
SET search_path TO public
AS $$
BEGIN
  RETURN EXISTS (
    SELECT 1 FROM users
    WHERE id = auth.uid() AND role = 'admin'
  );
END;
$$;

-- 3. 重新创建策略
CREATE POLICY "Users can read own data" ON users
  FOR SELECT USING (auth.uid() = id);

CREATE POLICY "Users can update own data" ON users
  FOR UPDATE USING (auth.uid() = id);

CREATE POLICY "Users can insert own data" ON users
  FOR INSERT WITH CHECK (auth.uid() = id);

CREATE POLICY "Admins can read all users" ON users
  FOR SELECT USING (is_admin());
```

执行后清除缓存并重新登录。

