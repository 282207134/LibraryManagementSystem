# 调试管理员界面（`/admin`）指南

适用现象：管理员进入 `/admin` 后无法正常加载，或被错误重定向。

## 1. 先确认前提

1. 当前账号在 `users` 表中存在  
2. `users.role = 'admin'`  
3. 已登录且会话未过期  

## 2. 浏览器侧排查

1. 打开开发者工具 Console/Network
2. 刷新 `/admin/dashboard`
3. 关注以下请求：
   - `users` 查询是否 401/403/500
   - `borrowing_records` 查询是否被策略拒绝

## 3. 数据库侧排查

在 Supabase SQL Editor 执行：

```sql
select is_admin();
select id, email, role from users where id = auth.uid();
```

如果 `is_admin()` 报错或返回异常，先修复函数与策略（见 `修复步骤说明.md`）。

## 4. 常见根因

- `users` 表缺少当前用户行
- `role` 不是 `admin`
- `is_admin()` 未创建或策略递归
- RLS 策略缺失/被覆盖

## 5. 最小修复动作

1. 重新执行 `修复RLS策略.sql`
1. 再执行一次：

```sql
select is_admin();
```

1. 退出登录后重新登录再访问 `/admin`

## 6. 仍失败时

- 清空浏览器站点数据后重试
- 检查是否连接了错误的 Supabase 项目（`VITE_SUPABASE_URL`）
- 检查部署环境是否加载了最新 SQL 变更
