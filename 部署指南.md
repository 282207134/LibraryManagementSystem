# 部署指南

本指南对应当前仓库的真实实现（用户端 + 管理端 + AI 助手）。

## 1. 前置要求

- Node.js 18+
- npm
- Supabase 账号与项目

## 2. 安装依赖

```bash
npm install
```

## 3. 配置 Supabase 基础环境

1. 在 Supabase 控制台创建项目  
2. 打开 `Authentication -> Providers`，启用 Email 登录  
3. 开发阶段可关闭 email confirmation（按团队策略决定）

## 4. 初始化数据库

推荐直接使用仓库中的 SQL 文件按顺序执行：

1. `MIGRATION.sql`（核心表与策略）
2. `USER_TABLES_MIGRATION.sql`（用户扩展与权限）
3. `FIX_BORROW_FUNCTION.sql`（借阅/归还 RPC 修正）
4. `STORAGE_SETUP.sql`（封面存储策略）

如果你只想先跑通核心流程，至少确保存在以下对象：

- 表：`books`、`users`、`borrowing_records`、`book_favorites`、`reviews`
- 函数：`is_admin()`、`borrow_book()`、`return_book()`
- Storage bucket：`book-covers`

## 5. 配置前端环境变量

根目录创建 `.env`：

```env
VITE_SUPABASE_URL=https://your-project-ref.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key
```

> 只使用 `anon` key，不要把 `service_role` 放到前端。

## 6. 本地启动

```bash
npm run dev
```

访问：`http://localhost:5173`

## 7. 部署 AI Edge Function（可选但推荐）

1. 复制并填写函数环境变量：

```bash
cp supabase/functions/.env.example supabase/functions/.env
```

1. 同步 secrets 到云端：

```bash
npm run supabase:cloud:secrets
```

1. 部署函数：

```bash
npm run supabase:functions:deploy
```

> 修改了 Supabase 项目后，记得同步更新 `package.json` 中脚本里的 `--project-ref`。

## 8. 前端生产部署

### Vercel

- Build Command: `npm run build`
- Output: `dist`
- 环境变量：`VITE_SUPABASE_URL`、`VITE_SUPABASE_ANON_KEY`

### Netlify

- Build Command: `npm run build`
- Publish Directory: `dist`
- 同样配置上述两个环境变量

## 9. 验证清单

- 注册/登录正常
- 用户端可浏览图书、借阅、归还、收藏、评论
- 管理员账号可访问 `/admin/dashboard` 与 `/admin/borrowings`
- 图书封面上传/替换/删除正常
- AI 助手可返回书目卡片并支持“下一批”

## 10. 常见问题

- `Could not find the table ...`
  - 核心表未建齐，重跑迁移 SQL
- `/admin` 一直加载
  - 检查 `users.role` 是否为 `admin`
  - 检查 `is_admin()` 与 RLS 策略
  - 见 `调试管理员界面.md`
- Edge Function 非 2xx
  - 检查 `supabase:functions:deploy` 是否成功
  - 检查 secrets 是否同步
  - 检查模型 API key 是否正确
